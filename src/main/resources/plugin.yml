name: DataSyncer
version: '${project.version}'
main: ru.mcfine.datasyncer.datasyncer.DataSyncer
depend: [EliteMobs]
api-version: 1.19
commands:
  emsync:
    description: Main command of the plugin.
    usage: Just use it.
